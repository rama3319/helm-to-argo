app:
  app:
    name: my-app
  image:
    repository: nginx
    tag: "1.27"
  replicaCount: 1
  containerPort: 80
  service:
    type: ClusterIP
    port: 80
  ingress:
    enabled: true
    className: traefik
    host: my-app.local
    path: /
    pathType: Prefix
